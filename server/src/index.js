// ============================================================
// 帷幕之地游戏服务器 入口 —— 独立运行模式
// ============================================================

import { startServer } from './app.js';

const PORT = process.env.PORT || 4000;
startServer(PORT);
